DECLARE (a) ARRAY [9] OF INTEGER;
DECLARE (i) INTEGER;
DECLARE (i2) INTEGER;
DECLARE (c) cHaR;

fill: PROCEDURE ((a) ARRAY [10] OF CHAR, (b, c) INTEGER);
	LOOP;
	ENDLOOP;
END fill;

START;
	i2 := a[5] + - 2;  
	
	IF i2 < 5 THEN
		i2 := 5;
	ELSE
		i2 := 0;
	ENDIF;	

	fill(1, 10, 0);
	PUT(5.5, .1, 2.);
END;
